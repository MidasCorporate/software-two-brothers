import User from '../schemas/User';

class UserController {
  async index(req, res) {
    return res.json();
  }

  async store(req, res) {
    return res.json();
  }
}

export default new UserController();
